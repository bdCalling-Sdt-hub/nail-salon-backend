<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Booking Confirmation</title>
    <style>
        body {
            font-family: Arial, sans-serif;
        }
        .container {
            padding: 20px;
            max-width: 600px;
            margin: auto;
            background-color: #f4f4f4;
            border-radius: 10px;
        }
        h1 {
            color: #333;
        }
        .details {
            margin-top: 20px;
        }
        .details p {
            margin: 10px 0;
        }
        .services ul {
            padding-left: 20px;
        }
        .services li {
            margin-bottom: 5px;
        }
        .footer {
            margin-top: 30px;
            text-align: center;
            font-size: 12px;
            color: #666;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Thank You for Booking with Us, <%= name %>!</h1>
        <p>We have received your booking request. Here are the details:</p>

        <div class="details">
            <div class="services">
                <p><strong>Services:</strong></p>
                <ul>
                <% services.forEach(function(service) { %>
                    <li><%= service.serviceName %></li>
                <% }); %>
                </ul>
            </div>
            <p><strong>Date:</strong> <%= date %></p>
            <p><strong>Time:</strong> <%= time %></p>
        </div>

        <div class="footer">
            <p>Best Regards,</p>
            <p><strong><%= salon %></strong></p>
            <p>Email: <%= email %> | Phone: <%= contact %></p>
        </div>
    </div>
</body>
</html>
